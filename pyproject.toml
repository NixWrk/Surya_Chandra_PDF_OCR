[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "uniscan"
version = "0.1.0"
description = "Hybrid Chandra-text and Surya-geometry OCR pipeline for searchable PDFs."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [
  { name = "img_2_pdf contributors" }
]
dependencies = [
  "opencv-python>=4.8",
  "numpy>=1.26",
  "pillow>=10.2",
  "img2pdf>=0.5",
  "pymupdf>=1.24"
]

[project.optional-dependencies]
ocr = [
  "ocrmypdf>=16.0",
  "pypdf>=4.0",
  "reportlab>=4.0"
]
dev = [
  "mypy>=1.10",
  "pytest>=8.0",
  "pytest-cov>=5.0",
  "ruff>=0.7",
  "types-reportlab"
]

[project.scripts]
uniscan = "uniscan.cli:main"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.mypy]
strict = true
files = ["src", "scripts"]

[[tool.mypy.overrides]]
# Optional OCR/PDF backends do not publish typed interfaces.
module = [
  "chandra",
  "chandra.*",
  "fitz",
  "fitz.*",
  "img2pdf",
  "mineru",
  "mineru.*",
  "paddleocr",
  "paddleocr.*",
  "surya",
  "surya.*",
]
ignore_missing_imports = true
